spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/community;
    username: sa
    password:
    driver-class-name: org.h2.Driver
  servlet:
    multipart:
      maxFileSize: 10MB
      maxRequestSize: 20MB

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug

server:
  servlet:
    session:
      timeout: 30m
      tracking-modes: cookie

file:
  dir: C:/Users/spa89/Downloads/develop/springCommuProject/file/